{% extends 'base.html.twig' %}
{% block title %}Dashboard Admin{% endblock %}

{% block body %}
<h1 class="mb-4"><i class="bi bi-speedometer2 text-warning"></i> Dashboard Admin</h1>

{# ─── Statistiques du jour ──────────────────────────────────────────────── #}
<div class="row g-3 mb-5">
    <div class="col-md-4">
        <div class="card text-center p-3">
            <div class="fs-1">🧾</div>
            <div class="fs-2 fw-bold text-warning">{{ commandes|length }}</div>
            <div class="text-muted">Commandes aujourd'hui</div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center p-3">
            <div class="fs-1">💶</div>
            <div class="fs-2 fw-bold text-success">{{ ca|number_format(2, ',', ' ') }} €</div>
            <div class="text-muted">Chiffre d'affaires du jour</div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-center p-3">
            <div class="fs-1">⏳</div>
            <div class="fs-2 fw-bold text-danger">
                {{ commandes|filter(c => c.statut == 'en_attente')|length }}
            </div>
            <div class="text-muted">En attente</div>
        </div>
    </div>
</div>

{# ─── Liens rapides ─────────────────────────────────────────────────────── #}
<div class="mb-4">
    <a href="{{ path('produit_new') }}" class="btn btn-orange me-2">
        <i class="bi bi-plus-circle"></i> Nouveau produit
    </a>
</div>

{# ─── Tableau des commandes du jour ────────────────────────────────────── #}
<h4 class="mb-3">Commandes du jour</h4>

{% if commandes is empty %}
    <div class="alert alert-info">Aucune commande aujourd'hui.</div>
{% else %}
<div class="table-responsive">
    <table class="table table-hover align-middle bg-white rounded shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>#</th>
                <th>Client</th>
                <th>Heure</th>
                <th>Total</th>
                <th>Statut</th>
                <th>Changer statut</th>
            </tr>
        </thead>
        <tbody>
            {% for commande in commandes %}
            <tr>
                <td>{{ commande.id }}</td>
                <td>{{ commande.client.nom ?? commande.client.email }}</td>
                <td>{{ commande.date|date('H:i') }}</td>
                <td class="fw-bold">{{ commande.total|number_format(2, ',', ' ') }} €</td>
                <td>
                    <span class="badge badge-{{ commande.statut }}">{{ commande.statutLabel }}</span>
                </td>
                <td>
                    {% if demoMode %}
                        <div class="d-flex gap-1">
                            <select class="form-select form-select-sm" style="width:140px;">
                                <option value="en_attente" {{ commande.statut == 'en_attente' ? 'selected' }}>En attente</option>
                                <option value="prete"      {{ commande.statut == 'prete'      ? 'selected' }}>Prête</option>
                                <option value="livree"     {{ commande.statut == 'livree'     ? 'selected' }}>Livrée</option>
                            </select>
                            <button type="button" class="btn btn-sm btn-orange"
                                    onclick="alert('Mode démo : modification du statut simulée');">
                                OK
                            </button>
                        </div>
                    {% else %}
                        <form method="post" action="{{ path('admin_commande_statut', {id: commande.id}) }}"
                            class="d-flex gap-1">
                            <select name="statut" class="form-select form-select-sm" style="width:140px;">
                                <option value="en_attente" {{ commande.statut == 'en_attente' ? 'selected' }}>En attente</option>
                                <option value="prete"      {{ commande.statut == 'prete'      ? 'selected' }}>Prête</option>
                                <option value="livree"     {{ commande.statut == 'livree'     ? 'selected' }}>Livrée</option>
                            </select>
                            <button class="btn btn-sm btn-orange">OK</button>
                        </form>
                    {% endif %}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endif %}
{% endblock %}
